import { createAction } from '@ngrx/store';

export const increment = createAction('');
export const decrement = createAction('[toto] Decrement');
export const reset = createAction('[Counter Component] Reset');
